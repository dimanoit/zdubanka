{
   "tasks": [
      {
         "name": "dotnet-format",
         "command": "dotnet format",
         "group": "pre-commit",
         "args": ["dotnet-format", "--include" , "${staged}"],
         "include": ["**/*.cs"]
      }
   ]
}
